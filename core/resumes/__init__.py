"""Resume library services (CRUD + queries).

Document *parsing* and *rendering* live in :mod:`core.documents` (Phase 6/7); this
package owns the resume records and their lifecycle in the library.
"""
